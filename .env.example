# Disabled by default. No provider/account/sender has been configured or approved.
ENQUIRY_ENABLED=false
# Server only: configure only after owner approval of provider, sender and retention.
RESEND_API_KEY=
ENQUIRY_FROM=
ENQUIRY_REDIS_URL=
ENQUIRY_REDIS_TOKEN=
ENQUIRY_HMAC_SECRET=
# Exact HTTPS origins, comma separated; add an approved preview URL explicitly.
ENQUIRY_ALLOWED_ORIGINS=
